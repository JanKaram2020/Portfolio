import MySandpack from "app/blog/components/MySandpack";
import {getFileCode} from "./utilitiy";
export const frontMatter = {
    title: 'Typescript type utilities',
    summary:'10 type utilities to help you write more robust typescript code',
    publishedAt: '2024-02-29',
    containsImports:["parameters.ts"]
};
export const ParametersCode = getFileCode("parameters.ts").replaceAll("// @ts-nocheck", "").split("/*split*/").map((s) => s.trim())

## Parameters
a utility that gives us the parameters of a function type as an array

<MySandpack template={"vanilla-ts"} editor={false} filesTab={false} files={{
    "parameters.ts": ParametersCode[0],
}} />
it's very useful when you have a already defined a function without separating the types
or you're using a library that doesn't export it's types

## ReturnType
a utility that gives us the parameters of a function type as an array

<MySandpack template={"vanilla-ts"} editor={false} filesTab={false} files={{
    "return-type.ts": ParametersCode[1],
}} />
it's very useful when you have a already defined a function without separating the types
or you're using a library that doesn't export it's types

## ReturnType
## Awaited
## Partial
## Required
## NonNullable
## typeof
## Pick
## Omit
## Dynamic String Type `${number}-Jan`
## Type Predicates
## Extract